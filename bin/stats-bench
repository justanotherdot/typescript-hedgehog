#!/bin/sh -eu

# Statistical benchmark script for hedgehog package
WORKSPACE_ROOT=$(git rev-parse --show-toplevel)
cd "$WORKSPACE_ROOT"

npm run bench --workspace=@justanotherdot/hedgehog -- src/seed/statistical-benchmark.bench.ts